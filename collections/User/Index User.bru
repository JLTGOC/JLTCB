meta {
  name: Index User
  type: http
  seq: 2
}

get {
  url: {{base_url}}/user
  body: json
  auth: inherit
}

headers {
  Accept: application/json
}

body:json {
  {
      "role": "AS "
  }
}

settings {
  encodeUrl: true
}
